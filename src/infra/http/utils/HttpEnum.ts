export enum HttpStatusCode {
    OK = 200,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
    SERVER_ERROR = 500,
}

export enum HttpStatusText {
    OK = 'ok',
    BAD_REQUEST = 'bad request',
    NOT_FOUND = 'not found',
    SERVER_ERROR = 'oooopssss, server error!',
}